export type FeaturedProjectsType = {
	client: string;
	media: [
		{
			webmVideoFile: {
				url: string;
			};
			mp4VideoFile: {
				url: string;
			};
			placeholderImage: {
				url: string;
			}
		}
	];
	title: string;
	vimeoLink: string;
};

export type IndexProjectsType = {
	letter: string;
	client: [];
};

export type MediaType = {
	media: [
		{
			webmVideoFile: {
				url: string;
			};
			mp4VideoFile: {
				url: string;
			};
			placeholderImage: {
				url: string;
			}
		}
	];
};
